# React 컴포넌트 설계서 — 인지기능검사 서비스

## 문서 정보
| 항목 | 내용 |
|------|------|
| **작성자** | 강찬식 서다영 황지영 |
| **작성일** | 2026-09-23 |
| **버전** | v1.0 |
| **검토자** | 강찬식 서다영 황지영 |

> 범위: 사용자 화면 SCR-01 ~ SCR-06 (관리자 화면은 Thymeleaf로 구현하므로 제외)
> 기술: React, React Router, Zustand, shadcn/ui, Recharts, sonner

---

## 0. 컴포넌트 설계 프로세스

### STEP 1 — 화면별 컴포넌트 추출

| 화면 | 추출한 컴포넌트 |
|---|---|
| 공통 | `Header`, `Footer`, `LoadingSpinner`, `ErrorAlert`, `RiskBadge` |
| SCR-01 로그인 | `LoginForm` |
| SCR-02 회원가입 | `RegisterForm` |
| SCR-03/04 검사 | `ProgressBar`, `QuestionCard`, `AnswerOptions`, `SurveyNavigation`, `SecondExamDialog` |
| SCR-05 결과 | `ResultStatus`, `ScoreSummary`, `Recommendation` |
| SCR-06 마이페이지 | `ProfileCard`, `ExamHistoryTable`, `ExamTrendChart` |

> SCR-03(KDSQ-P)과 SCR-04(KDSQ-C)는 구조가 같아 `SurveyPage` 하나로 만들고 `type` Props로 구분합니다.

### STEP 2 — 컴포넌트 트리

```
App
  └── UserLayout                        ← Header + Outlet + Footer
        ├── LoginPage        /login            (State: loading, error)
        │     └── LoginForm                    (Props: onSubmit, loading, error)
        ├── RegisterPage     /register         (State: loading, error)
        │     └── RegisterForm                 (Props: onSubmit, loading, error)
        └── ProtectedRoute                     ← 비로그인 시 /login
              ├── SurveyPage /surveys, /surveys/c  (Props: type) (State: currentIndex, answers)
              │     ├── ProgressBar            (Props: current, total)
              │     ├── QuestionCard           (Props: question, number)
              │     │     └── AnswerOptions    (Props: value, onChange)
              │     ├── SurveyNavigation       (Props: isFirst, isLast, onPrev, onNext, onSubmit)
              │     └── SecondExamDialog       (Props: open, firstScore, onConfirm)
              ├── ResultPage /results/:examId  (State: examStore)
              │     ├── ResultStatus           (Props: exam)
              │     ├── ScoreSummary           (Props: memoryScore, otherScore, adlScore)
              │     └── Recommendation         (Props: riskLevel)
              └── MyPage     /mypage           (State: memberStore, examStore)
                    ├── ProfileCard            (Props: member)
                    ├── ExamHistoryTable       (Props: exams, onDetail)
                    │     └── RiskBadge        (Props: exam)
                    └── ExamTrendChart         (Props: exams)
```

### STEP 3 — 상태 위치 결정

| 상태 | 위치 | 이유 |
|------|------|------|
| `token` | `authStore` (persist) | 앱 전체 인증 확인, 새로고침 후 유지 |
| `questions` | `surveyStore` | 서버 데이터 |
| `currentExam`, `history` | `examStore` | 서버 데이터, 결과·마이페이지에서 사용 |
| `pendingPExamId` | `examStore` (sessionStorage) | 1차 4점 이상일 때만 2차 검사 진입 허용 |
| `me` | `memberStore` | 서버 데이터 |
| `currentIndex`, `answers` | `SurveyPage` 로컬 | 검사 화면 안에서만 사용, 응답은 저장하지 않음 |
| 폼 입력값, 모달 열림 | 각 컴포넌트 로컬 | UI 상태 |

---

## 1. 컴포넌트 개요

| 화면 ID | 페이지 컴포넌트 | URL | 주요 기능 |
|---|---|---|---|
| SCR-01 | `LoginPage` | `/login` | 로그인 → `/surveys` 이동 |
| SCR-02 | `RegisterPage` | `/register` | 회원가입 → `/login` 이동 |
| SCR-03 | `SurveyPage type="P"` | `/surveys` | 5문항, 0~3점 결과 / 4~10점 2차 안내 |
| SCR-04 | `SurveyPage type="C"` | `/surveys/c` | 15문항, 영역별 점수 산출 |
| SCR-05 | `ResultPage` | `/results/:examId` | 총점·판정·영역별 점수·관리 안내 |
| SCR-06 | `MyPage` | `/mypage` | 회원정보 조회, 검사 이력, 추이 차트 |

---

## 2. 컴포넌트 구조 설계

### 2.1 파일 구조
```
src/
├── pages/
│   ├── Login/          index.jsx, components/LoginForm.jsx
│   ├── Register/       index.jsx, components/RegisterForm.jsx
│   ├── Survey/         index.jsx, components/(ProgressBar, QuestionCard, AnswerOptions,
│   │                                           SurveyNavigation, SecondExamDialog)
│   ├── Result/         index.jsx, components/(ResultStatus, ScoreSummary, Recommendation)
│   └── MyPage/         index.jsx, components/(ProfileCard, ExamHistoryTable, ExamTrendChart)
├── components/
│   ├── layout/         UserLayout.jsx, Header.jsx, Footer.jsx
│   └── common/         ProtectedRoute.jsx, LoadingSpinner.jsx, ErrorAlert.jsx, RiskBadge.jsx
├── store/              authStore.js, surveyStore.js, examStore.js, memberStore.js
├── api/                client.js, authApi.js, surveyApi.js, examApi.js, memberApi.js
└── utils/kdsq.js       점수 계산·판정 함수
```

---

## 3. Props 설계

### 3.1 도메인 타입
```typescript
type Member = { id: number; name: string; email: string; gender: 'MALE' | 'FEMALE'; birthYear: number };

type Question = { id: number; order: number; content: string };

type Exam = {
  examId: number;
  examType: 'KDSQ_P' | 'KDSQ_C';
  firstScore: number | null;                  // P (0~10)
  memoryScore: number | null;                 // C (0~10)
  otherScore: number | null;                  // C (0~10)
  adlScore: number | null;                    // C (0~10)
  riskLevel: 'Normal' | 'Borderline' | 'HighRisk';
  createdAt: string;
};
// totalScore는 저장하지 않고 프론트에서 계산
```

### 3.2 주요 컴포넌트 Props

| 컴포넌트 | Props | 타입 | 필수 | 설명 |
|---|---|---|---|---|
| `LoginForm` | `onSubmit` | `(email, password) => void` | 필수 | 검증 후 호출 |
| | `loading` / `error` | `boolean` / `string` | 선택 | 제출 상태, 오류 문구 |
| `RegisterForm` | `onSubmit` | `(form) => void` | 필수 | 검증 후 호출 |
| | `loading` / `error` | `boolean` / `string` | 선택 | 409 중복 이메일 등 |
| `SurveyPage` | `type` | `'P' \| 'C'` | 필수 | 문항·제출 API 결정 |
| `ProgressBar` | `current`, `total` | `number` | 필수 | 진행률 표시 |
| `QuestionCard` | `question`, `number` | `Question`, `number` | 필수 | 현재 문항 |
| `AnswerOptions` | `value` | `number \| null` | 필수 | 선택값 (0/1/2) |
| | `onChange` | `(value) => void` | 필수 | 선택 시 호출 |
| `SurveyNavigation` | `isFirst`, `isLast` | `boolean` | 필수 | 이전 비활성화 / 제출 버튼 표시 |
| | `onPrev`, `onNext`, `onSubmit` | `() => void` | 필수 | 문항 이동, 제출 |
| `SecondExamDialog` | `open`, `firstScore` | `boolean`, `number` | 필수 | 2차 안내 모달 |
| | `onConfirm` | `() => void` | 필수 | `/surveys/c` 이동 |
| `ResultStatus` | `exam` | `Exam` | 필수 | 판정 문구 + 총점 |
| `ScoreSummary` | `memoryScore`, `otherScore`, `adlScore` | `number` | 필수 | KDSQ-C만 표시 |
| `Recommendation` | `riskLevel` | `string` | 필수 | 관리·추가 검진 안내 (KDSQ-C만) |
| `ProfileCard` | `member` | `Member` | 필수 | 조회 전용 |
| `ExamHistoryTable` | `exams` | `Exam[]` | 필수 | 최신순 목록 |
| | `onDetail` | `(examId) => void` | 필수 | 결과 화면 이동 |
| `ExamTrendChart` | `exams` | `Exam[]` | 필수 | P/C 라인 분리 표시 |
| `RiskBadge` | `exam` | `Exam` | 필수 | 정상/주의/위험 배지 |

---

## 4. 상태 관리 설계

### 4.1 로컬 상태 (SurveyPage)
| 상태명 | 타입 | 초기값 | 용도 |
|---|---|---|---|
| `currentIndex` | `number` | `0` | 현재 문항 |
| `answers` | `number[]` | `[]` | 문항별 응답 (제출 시 점수로 환산) |
| `validationError` | `string` | `''` | "답변을 선택해주세요." |
| `isDialogOpen` | `boolean` | `false` | 2차 안내 모달 |

### 4.2 Zustand 스토어
| 스토어 | 상태 | 액션 |
|---|---|---|
| `authStore` | `token`, `email` | `login()`, `logout()` |
| `surveyStore` | `questions`, `loading`, `error` | `fetchQuestions(type)` |
| `examStore` | `currentExam`, `history`, `pendingPExamId`, `loading` | `submitP()`, `submitC()`, `fetchExam(id)`, `fetchHistory()` |
| `memberStore` | `me`, `loading` | `fetchMe()` |

### 4.3 점수·판정 규칙 (`utils/kdsq.js`)
```
firstScore  = Q1~Q5 합계                → 0~3 정상 / 4~10 2차 대상
memoryScore = Q1~Q5,  otherScore = Q6~Q10,  adlScore = Q11~Q15
totalScore  = memory + other + adl      → 0~5 주의 / 6~30 고위험군
```

---

## 5. API 연동 설계

### 5.1 API 목록
| API | 메서드 | 용도 | 호출 시점 |
|---|---|---|---|
| `/api/auth/login` | POST | 로그인 | 로그인 버튼 |
| `/api/auth/register` | POST | 회원가입 | 가입 버튼 |
| `/api/auth/logout` | POST | 로그아웃 | Header 로그아웃 |
| `/api/surveys/p`, `/api/surveys/c` | GET | 문항 조회 | 검사 화면 진입 |
| `/api/exams/p`, `/api/exams/c` | POST | 검사 결과 저장 | 마지막 문항 제출 |
| `/api/exams/:examId` | GET | 결과 조회 | 결과 화면 진입 |
| `/api/exams/history` | GET | 검사 이력 | 마이페이지 진입 |
| `/api/members/me` | GET | 회원정보 | 마이페이지 진입 |

> 로그인·회원가입 외 모든 API는 `Authorization: Bearer {token}` 필요 (axios 인터셉터에서 자동 첨부)

### 5.2 에러 처리
| 상태 | 처리 |
|---|---|
| 400 | 폼 오류 메시지 |
| 401 | 로그인 API: "이메일 또는 비밀번호를 확인해주세요." / 그 외: 로그아웃 후 `/login` |
| 404 | "검사 결과를 찾을 수 없습니다." |
| 409 | "이미 가입된 이메일입니다." |
| 500 | "서버 오류. 잠시 후 다시 시도해 주세요" + 재시도 |

---

## 6. 컴포넌트 구현 골격 — SurveyPage

```jsx
// src/pages/Survey/index.jsx
export default function SurveyPage({ type }) {

  // ── 1. 로컬 상태 ─────────────────────────────
  const [currentIndex, setCurrentIndex] = useState(0);
  const [answers, setAnswers] = useState([]);
  const [validationError, setValidationError] = useState('');
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  // ── 2. Zustand 전역 상태 ─────────────────────
  const { questions, loading, fetchQuestions } = useSurveyStore();
  const { submitP, submitC, pendingPExamId } = useExamStore();
  const navigate = useNavigate();

  // ── 3. 사이드 이펙트 ─────────────────────────
  useEffect(() => {
    if (type === 'C' && !pendingPExamId) { navigate('/surveys', { replace: true }); return; }
    fetchQuestions(type);
  }, [type]);

  // ── 4. 이벤트 핸들러 ─────────────────────────
  const handleNext = () => {
    if (answers[currentIndex] == null) return setValidationError('답변을 선택해주세요.');
    setCurrentIndex((i) => i + 1);
  };

  const handleSubmit = async () => {
    if (type === 'P') {
      const exam = await submitP(calcFirstScore(answers));
      exam.firstScore >= 4 ? setIsDialogOpen(true) : navigate(`/results/${exam.examId}`);
    } else {
      const exam = await submitC(calcCScores(answers));
      navigate(`/results/${exam.examId}`);
    }
  };

  // ── 5. 조건부 렌더링 ─────────────────────────
  if (loading) return <LoadingSpinner />;

  // ── 6. 렌더링 ────────────────────────────────
  return (
    <div className="container mx-auto max-w-2xl py-6 space-y-6">
      <ProgressBar current={currentIndex + 1} total={questions.length} />
      <QuestionCard question={questions[currentIndex]} number={currentIndex + 1}>
        <AnswerOptions value={answers[currentIndex]} onChange={handleSelect} />
      </QuestionCard>
      <SurveyNavigation
        isFirst={currentIndex === 0}
        isLast={currentIndex === questions.length - 1}
        onPrev={() => setCurrentIndex((i) => i - 1)}
        onNext={handleNext}
        onSubmit={handleSubmit}
      />
      <SecondExamDialog open={isDialogOpen} onConfirm={() => navigate('/surveys/c')} />
    </div>
  );
}
```

---

## 7. 성능 최적화

- [x] `React.memo` — `QuestionCard`, `AnswerOptions`, `ExamTrendChart`
- [x] `useCallback` — `AnswerOptions`에 전달하는 `handleSelect`
- [x] `useMemo` — 마이페이지 이력 정렬, 차트 데이터 변환

---

## 8. 작성 체크리스트

- [x] STEP 1: 화면별 컴포넌트 경계를 나눴는가?
- [x] STEP 2: 컴포넌트 트리가 작성되었는가?
- [x] STEP 3: 상태 위치의 근거가 있는가?
- [x] Props의 타입과 필수여부가 작성되었는가?
- [x] API와 호출 시점이 명확한가?
- [x] 로딩 / 에러 / 빈 데이터 상태를 고려했는가?