# React 컴포넌트 설계서 — 인지기능검사 서비스

## 문서 정보
| 항목 | 내용 |
|------|------|
| **프로젝트명** | KDSQ 인지선별검사 플랫폼 (KDSQ Cognitive Screening) |
| **작성자** | 강찬식 서다영 황지영 |
| **작성일** | 2026-09-23 |
| **버전** | v2.0 |
| **검토자** | 강찬식 서다영 황지영 |

> 범위: 사용자 화면 SCR-01 – SCR-06 (관리자 화면은 Thymeleaf로 구현하므로 제외)
> 기술: React + Vite, React Router, Zustand, Axios, shadcn/ui, Recharts, sonner

---

## 1. 컴포넌트 개요

### 1.1 컴포넌트 목록

| 화면 ID | 페이지 컴포넌트 | URL | 주요 기능 |
|---|---|---|---|
| SCR-01 | `LoginPage` | `/login` | 로그인 → `/surveys` 이동 |
| SCR-02 | `RegisterPage` | `/register` | 회원가입 → `/login` 이동 |
| SCR-03 | `SurveyPage type="P"` | `/surveys` | 5문항, 0–3점 결과 / 4–10점 2차 안내 |
| SCR-04 | `SurveyPage type="C"` | `/surveys/c` | 15문항, 영역별 점수 산출 |
| SCR-05 | `ResultPage` | `/results/:examId` | 총점·판정·영역별 점수·관리 안내 |
| SCR-06 | `MyPage` | `/mypage` | 회원정보 조회, 검사 이력, 추이 차트 |

> 도메인/화면 설계서 기반 URL 적용 필요

### 1.2 목적 및 역할

| 컴포넌트 | 주요 목적 | 위치 | 유형 |
|---|---|---|---|
| `UserLayout` | Header/Footer 공통 배치, `<Outlet />` 렌더링 | `src/components/layout/UserLayout.jsx` | 레이아웃 |
| `ProtectedRoute` | 토큰 없으면 `/login`으로 리다이렉트 | `src/components/common/ProtectedRoute.jsx` | 공통 |
| `LoginPage` | 로그인 API 호출, 성공 시 `/surveys` 이동 | `src/pages/Login/index.jsx` | 페이지 |
| `RegisterPage` | 회원가입 API 호출, 성공 시 `/login` 이동 | `src/pages/Register/index.jsx` | 페이지 |
| `SurveyPage` | P/C 문항 진행, 점수 산출, 제출 및 분기 | `src/pages/Survey/index.jsx` | 페이지 |
| `ResultPage` | 검사 점수·판정·관리 안내 표시 | `src/pages/Result/index.jsx` | 페이지 |
| `MyPage` | 회원정보(조회 전용), 검사 이력, 추이 | `src/pages/MyPage/index.jsx` | 페이지 |

### 1.3 기능 요구사항

**SCR-01 로그인**
- [ ] 이메일·비밀번호 검증, 로그인 중 버튼 비활성화
- [ ] 실패(401) 시 오류 문구, 성공 시 토큰 저장 → `/surveys`

**SCR-02 회원가입**
- [ ] 필드별 유효성 검증 및 오류 표시
- [ ] 이메일 중복(409) 안내, 성공 시 → `/login`

**SCR-03/04 검사**
- [ ] 문항 조회 후 한 문항씩 표시 + 진행률, 이전/다음 이동(응답 유지)
- [ ] 미선택 시 "답변을 선택해주세요.", 제출 중 버튼 비활성화
- [ ] KDSQ-P: 0–3점 → 결과 화면 / 4–10점 → 2차 안내 → `/surveys/c`
- [ ] KDSQ-C: 영역별 점수 산출·제출 → 결과 화면 (2차 대상 아니면 접근 차단)

**SCR-05 결과**
- [ ] 총점·판정 표시 (KDSQ-C는 영역별 점수 + 관리 안내 추가)
- [ ] 조회 실패 시 재시도, [검사 이력 보기] → `/mypage`

**SCR-06 마이페이지**
- [ ] 회원정보 조회 전용 표시
- [ ] 검사 이력 목록(최신순) + 점수 추이 그래프
- [ ] 이력 없음 → 안내 문구 + 검사 시작 버튼

---

## 2. 컴포넌트 구조 설계

### 2.1 컴포넌트 분류

| 컴포넌트 | 타입 | 복잡도 | 상태 관리 |
|---|---|---|---|
| `LoginPage` | 페이지 | 단순 | Zustand 연결 (`authStore`) + 로컬 |
| `RegisterPage` | 페이지 | 단순 | 로컬 상태만 |
| `SurveyPage` | 페이지 | 복합 | Zustand 연결 (`surveyStore`, `examStore`) + 로컬 |
| `ResultPage` | 페이지 | 보통 | Zustand 연결 (`examStore`) |
| `MyPage` | 페이지 | 보통 | Zustand 연결 (`memberStore`, `examStore`) |
| `UserLayout`, `Footer` | 레이아웃 | 단순 | 상태 없음 |
| `Header` | 레이아웃 | 단순 | Zustand 연결 (`authStore`) |
| `LoginForm` | 기능 | 보통 | 로컬 상태만 (입력값) |
| `RegisterForm` | 기능 | 보통 | 로컬 상태만 (입력값) |
| `ProgressBar`, `QuestionCard`, `AnswerOptions`, `SurveyNavigation`, `SecondExamDialog` | 기능 | 단순 | 상태 없음 |
| `ResultStatus`, `ScoreSummary`, `Recommendation` | 기능 | 단순 | 상태 없음 |
| `ProfileCard`, `ExamHistoryTable`, `ExamTrendChart` | 기능 | 단순 | 상태 없음 |
| `ProtectedRoute` | 공통 | 단순 | Zustand 연결 (`authStore`) |
| `RiskBadge`, `LoadingSpinner`, `ErrorAlert` | 공통 | 단순 | 상태 없음 |

### 2.2 컴포넌트 트리 (계층 구조)

```
UserLayout                             ← 공통 레이아웃 (Header + Outlet + Footer)
  ├── LoginPage                        ← 상태 소유자, 로그인 API 호출
  │     └── LoginForm                  ← (Props: onSubmit, isLoading, errorMessage) 입력·검증
  ├── RegisterPage                     ← 상태 소유자, 회원가입 API 호출
  │     └── RegisterForm               ← (Props: onSubmit, isLoading, errorMessage) 입력·검증
  └── ProtectedRoute                   ← 비로그인 시 /login 이동
        ├── SurveyPage                 ← 상태 소유자, 문항 조회·결과 제출 (P/C 공용)
        │     ├── ProgressBar          ← (Props: current, total) 진행률
        │     ├── QuestionCard         ← (Props: question) 문항 표시
        │     │     └── AnswerOptions  ← (Props: value, onChange) 답변 선택
        │     ├── SurveyNavigation     ← (Props: isFirst, isLast, handlers) 이전/다음/제출
        │     └── SecondExamDialog     ← (Props: isOpen, onConfirm) 2차 검사 안내 모달
        ├── ResultPage                 ← 상태 소유자, 결과 조회 API 호출
        │     ├── ResultStatus         ← (Props: exam) 총점·판정
        │     ├── ScoreSummary         ← (Props: exam) 영역별 점수 (KDSQ-C만)
        │     └── Recommendation       ← (Props: riskLevel) 관리 안내 (KDSQ-C만)
        └── MyPage                     ← 상태 소유자, 회원정보·이력 조회 API 호출
              ├── ProfileCard          ← (Props: member) 회원정보 조회 전용
              ├── ExamHistoryTable     ← (Props: exams, onDetail) 검사 이력 목록
              │     └── RiskBadge      ← (Props: exam) 판정 배지
              └── ExamTrendChart       ← (Props: exams) 점수 추이 그래프
```

**컴포넌트별 책임 요약:**

| 컴포넌트명 | 파일 위치 | 주요 책임 | 재사용성 |
|---|---|---|---|
| `LoginPage` | `pages/Login/index.jsx` | 로그인 처리, 자식 조립 | 낮음 |
| `RegisterPage` | `pages/Register/index.jsx` | 회원가입 처리, 자식 조립 | 낮음 |
| `SurveyPage` | `pages/Survey/index.jsx` | 검사 진행 상태, 점수 산출·제출 | 보통 (P/C 공용) |
| `ResultPage` | `pages/Result/index.jsx` | 결과 조회, 자식 조립 | 낮음 |
| `MyPage` | `pages/MyPage/index.jsx` | 회원정보·이력 조회, 자식 조립 | 낮음 |
| `LoginForm`, `RegisterForm` | `pages/[Name]/components/` | 입력·유효성 검증 | 보통 |
| `ProgressBar`, `QuestionCard`, `AnswerOptions`, `SurveyNavigation`, `SecondExamDialog` | `pages/Survey/components/` | 검사 화면 표시 | 보통 |
| `ResultStatus`, `ScoreSummary`, `Recommendation` | `pages/Result/components/` | 결과 표시 | 보통 |
| `ProfileCard`, `ExamHistoryTable`, `ExamTrendChart` | `pages/MyPage/components/` | 마이페이지 표시 | 보통 |
| `RiskBadge` | `components/common/` | 판정 배지 (결과·마이페이지 공유) | 높음 |
| `Header`, `Footer`, `UserLayout` | `components/layout/` | 공통 레이아웃 | 높음 |
| `ProtectedRoute`, `LoadingSpinner`, `ErrorAlert` | `components/common/` | 접근 제어, 로딩·오류 표시 | 높음 |

### 2.3 파일 구조

```
src/
├── pages/
│   └── [Login | Register | Survey | Result | MyPage]/
│       ├── index.jsx                  # 페이지 컴포넌트 (상태 소유)
│       └── components/                # 이 페이지에서만 쓰는 컴포넌트
├── components/
│   ├── layout/                        # UserLayout, Header, Footer
│   └── common/                        # ProtectedRoute, LoadingSpinner, ErrorAlert, RiskBadge
├── store/                             # auth, survey, exam, member Store
├── api/                               # axiosInstance + auth, survey, exam, member Api
└── utils/kdsq.js                      # 점수 계산·판정 함수
```

---

## 3. Props 설계

### 3.1 Props 인터페이스

| 컴포넌트 | Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|---|
| `LoginForm` | `onSubmit` | `(email, password) => void` | 필수 | — | 검증 통과 후 호출 |
| | `isLoading` | `boolean` | 선택 | `false` | 로그인 중 버튼 비활성화 |
| | `errorMessage` | `string` | 선택 | `''` | 로그인 실패 문구 |
| `RegisterForm` | `onSubmit` | `(form) => void` | 필수 | — | 검증 통과 후 호출 |
| | `isLoading` | `boolean` | 선택 | `false` | 가입 중 버튼 비활성화 |
| | `errorMessage` | `string` | 선택 | `''` | 이메일 중복 등 서버 오류 문구 |
| `SurveyPage` | `type` | `'P' \| 'C'` | 필수 | — | 검사 종류 (문항·제출 API 결정) |
| `ProgressBar` | `current` | `number` | 필수 | — | 현재 문항 번호 |
| | `total` | `number` | 필수 | — | 전체 문항 수 (5 / 15) |
| `QuestionCard` | `question` | `Question` | 필수 | — | 현재 문항 |
| `AnswerOptions` | `value` | `number \| null` | 필수 | — | 선택한 답변 (0/1/2) |
| | `onChange` | `(value) => void` | 필수 | — | 답변 선택 시 호출 |
| `SurveyNavigation` | `isFirst` | `boolean` | 선택 | `false` | true면 [이전] 비활성화 |
| | `isLast` | `boolean` | 선택 | `false` | true면 [제출] 버튼 표시 |
| | `handlers` | `{ onPrev, onNext, onSubmit }` | 필수 | — | 문항 이동·제출 시 호출 |
| `SecondExamDialog` | `isOpen` | `boolean` | 선택 | `false` | 모달 열림 여부 |
| | `onConfirm` | `() => void` | 필수 | — | [2차 검사 시작] 클릭 시 호출 |
| `ResultStatus` | `exam` | `Exam` | 필수 | — | 총점·판정 표시 |
| `ScoreSummary` | `exam` | `Exam` | 필수 | — | 영역별 점수 표시 (KDSQ-C만) |
| `Recommendation` | `riskLevel` | `string` | 필수 | — | 판정별 관리 안내 (KDSQ-C만) |
| `ProfileCard` | `member` | `Member` | 필수 | — | 회원정보 표시 (조회 전용) |
| `ExamHistoryTable` | `exams` | `Exam[]` | 필수 | — | 검사 이력 (최신순) |
| | `onDetail` | `(examId) => void` | 필수 | — | [보기] 클릭 시 호출 |
| `ExamTrendChart` | `exams` | `Exam[]` | 필수 | — | 점수 추이 데이터 |
| `RiskBadge` | `exam` | `Exam` | 필수 | — | 정상/주의/위험 배지 |

> **도메인 타입 정의 (공통 참조)**
> ```typescript
> // Member 타입
> type Member = {
>   id: number;          // 회원 고유 ID
>   name: string;        // 이름
>   email: string;       // 이메일
>   gender: 'MALE' | 'FEMALE';  // 성별
>   birthYear: number;   // 출생년도
> }
>
> // Question 타입
> type Question = {
>   id: number;          // 문항 고유 ID
>   order: number;       // 문항 순서 (C: 1-5 기억력 / 6-10 기타인지 / 11-15 ADL)
>   content: string;     // 문항 내용
> }
>
> // Exam 타입
> type Exam = {
>   examId: number;
>   examType: 'KDSQ_P' | 'KDSQ_C';   // 검사 종류
>   firstScore: number | null;       // KDSQ-P 점수 (0-10)
>   memoryScore: number | null;      // KDSQ-C 기억력 (0-10)
>   otherScore: number | null;       // KDSQ-C 기타 인지기능 (0-10)
>   adlScore: number | null;         // KDSQ-C 일상생활수행능력 (0-10)
>   riskLevel: 'Normal' | 'Borderline' | 'HighRisk';  // 판정
>   createdAt: string;               // 검사일시
> }
> // totalScore는 저장하지 않고 프론트에서 계산
> ```

---

## 4. 상태 관리 설계

### 4.1 로컬 상태 (SurveyPage)

| 상태명 | 타입 | 초기값 | 용도 |
|---|---|---|---|
| `currentIndex` | `number` | `0` | 현재 문항 |
| `answers` | `number[]` | `[]` | 문항별 응답 (제출 시 점수로 환산) |
| `validationError` | `string` | `''` | "답변을 선택해주세요." |
| `isDialogOpen` | `boolean` | `false` | 2차 안내 모달 |

### 4.2 Zustand 스토어

| 스토어 | 상태 | 액션 |
|---|---|---|
| `authStore` | `token`, `email` | `login()`, `logout()` |
| `surveyStore` | `questions`, `loading`, `error` | `fetchQuestions(type)` |
| `examStore` | `currentExam`, `history`, `pendingPExamId`, `loading` | `submitP()`, `submitC()`, `fetchExam(id)`, `fetchHistory()` |
| `memberStore` | `me`, `loading` | `fetchMe()` |

> 비동기 액션은 `set({ loading: true })` → `try` API 호출 → `catch` 에러 저장 후 `throw` → `finally` 로딩 해제 순서로 작성하고, 오류 알림(toast)은 컴포넌트에서 처리합니다.

### 4.3 점수·판정 규칙 (`utils/kdsq.js`)

```
firstScore  = Q1-Q5 합계                → 0-3 정상 / 4-10 2차 대상
memoryScore = Q1-Q5,  otherScore = Q6-Q10,  adlScore = Q11-Q15
totalScore  = memory + other + adl      → 0-5 주의 / 6-30 고위험군
```

---

## 5. API 연동 설계

### 5.1 API 목록

| API | 메서드 | 용도 | 호출 시점 |
|---|---|---|---|
| `/api/auth/login` | POST | 로그인 | 로그인 버튼 |
| `/api/auth/register` | POST | 회원가입 | 가입 버튼 |
| `/api/auth/logout` | POST | 로그아웃 | Header 로그아웃 |
| `/api/surveys/p`, `/api/surveys/c` | GET | 문항 조회 | 검사 화면 진입 |
| `/api/exams/p`, `/api/exams/c` | POST | 검사 결과 저장 | 마지막 문항 제출 |
| `/api/exams/:examId` | GET | 결과 조회 | 결과 화면 진입 |
| `/api/exams/history` | GET | 검사 이력 | 마이페이지 진입 |
| `/api/members/me` | GET | 회원정보 | 마이페이지 진입 |

> 로그인·회원가입 외 모든 API는 `Authorization: Bearer {token}` 필요 (`axiosInstance.js` 인터셉터에서 자동 첨부)

### 5.2 에러 처리

| 상태 | 처리 |
|---|---|
| 400 | 폼 오류 메시지 |
| 401 | 로그인 API: "이메일 또는 비밀번호를 확인해주세요." / 그 외: 로그아웃 후 `/login` |
| 404 | "검사 결과를 찾을 수 없습니다." |
| 409 | "이미 가입된 이메일입니다." |
| 500 | "서버 오류. 잠시 후 다시 시도해 주세요" + 재시도 |

---

## 6. 컴포넌트 구현 골격

> 상세 구현은 담당 파트원이 작성합니다. 모든 페이지 컴포넌트는 아래 주석 순서(로컬 상태 → Zustand → useEffect → 핸들러 → 조건부 렌더링 → 렌더링)를 따릅니다.

```jsx
// src/pages/[PageName]/index.jsx

export default function [ComponentName]({ /* Props */ }) {

  // ── 1. 로컬 상태 ─────────────────────────────

  // ── 2. Zustand 전역 상태 ─────────────────────

  // ── 3. 사이드 이펙트 (데이터 로드) ───────────

  // ── 4. 이벤트 핸들러 ─────────────────────────

  // ── 5. 조건부 렌더링 (로딩/에러/빈 데이터) ───

  // ── 6. 렌더링 ────────────────────────────────
  return (
    <div>
      {/* 하위 컴포넌트 배치 */}
    </div>
  );
}
```

---

## 7. 성능 최적화

- [x] `React.memo` — `QuestionCard`, `AnswerOptions`, `ExamTrendChart`
- [x] `useCallback` — `AnswerOptions`에 전달하는 `handleSelect`
- [x] `useMemo` — 마이페이지 이력 정렬, 차트 데이터 변환

---

## 8. 작성 체크리스트

- [x] STEP 1: 화면별 컴포넌트 경계를 나눴는가?
- [x] STEP 2: 컴포넌트 트리가 작성되었는가?
- [x] STEP 3: 상태 위치의 근거가 있는가?
- [x] Props의 타입, 필수여부, 기본값이 작성되었는가?
- [x] API와 호출 시점이 명확한가?
- [x] 로딩 / 에러 / 빈 데이터 상태를 고려했는가?