# React 컴포넌트 설계서 — 인지기능검사 서비스 (사용자 화면)

## 문서 정보
| 항목 | 내용 |
|------|------|
| **작성자** | 강찬식 서다영 황지영 |
| **작성일** | 2026-09-23 |
| **버전** | v1.0 |
| **검토자** | 강찬식 서다영 황지영 |
| **기준 문서** | 4.UI화면설계서_2조_v3.0 |
| **설계 범위** | 사용자(User) 화면 SCR-01 ~ SCR-06 + 공통 컴포넌트 |
| **범위 제외** | 관리자(Admin) 화면 ADM-01 ~ ADM-06 — Thymeleaf(SSR)로 구현하므로 본 React 설계서 대상 아님 |

### 기술 스택
| 구분 | 사용 기술 |
|------|-----------|
| UI 라이브러리 | React (JSX) |
| 라우팅 | React Router |
| 전역 상태 | Zustand (+ `persist` 미들웨어) |
| UI 컴포넌트 | shadcn/ui (Card, Input, Button, RadioGroup, Progress, Alert, Badge, Table, Dialog) |
| 차트 | Recharts (`LineChart`) — 마이페이지 검사 추이 |
| 알림 | sonner (`toast`) |
| HTTP | axios (공통 인스턴스 + 인터셉터) |

---

## 0. 컴포넌트 설계 프로세스 (적용 결과)

### STEP 1 — 화면 설계서에서 사각형 그리기

UI 화면설계서 와이어프레임을 기준으로, 분리 기준 ① 재사용 ② 독립 변경 ③ 길이(100줄)를 적용해 컴포넌트 경계를 나눴습니다.

**① 공통 레이아웃 (모든 사용자 화면)**
```
┌──────────────────────────────────────────────────┐
│ [Header]  로고 / 검사하기 / 마이페이지 / 로그인·로그아웃 │  ← 모든 페이지 공통
├──────────────────────────────────────────────────┤
│              <Outlet /> (각 페이지)                │
├──────────────────────────────────────────────────┤
│ [Footer]  ⓒ 2026 마음 기억                         │  ← 모든 페이지 공통
└──────────────────────────────────────────────────┘
```

**② SCR-01 로그인 / SCR-02 회원가입**
```
┌──────────────────────────────────────────────────┐
│ [LoginPage]                                       │  ← 상태 소유 + API 호출 + 이동
│ ┌──────────────────────────────────────────────┐ │
│ │ [LoginForm]  이메일 / 비밀번호 / 버튼 / 오류    │ │  ← 입력·검증만 담당
│ └──────────────────────────────────────────────┘ │
│   [Link → /register]                              │
└──────────────────────────────────────────────────┘
  RegisterPage ─ RegisterForm 도 동일한 구조
```

**③ SCR-03 KDSQ-P / SCR-04 KDSQ-C — 같은 구조이므로 하나의 `SurveyPage`로 재사용**
```
┌──────────────────────────────────────────────────┐
│ [SurveyHeader]  검사명 + 안내 문구                 │  ← type(P/C)에 따라 문구만 다름
│ [ProgressBar]   진행률 7 / 15                      │  ← 독립
│ ┌──────────────────────────────────────────────┐ │
│ │ [QuestionCard]  Q7. 문항 내용                  │ │  ← 문항 변경 시 이 영역만 갱신
│ │   [AnswerOptions] ○ 전혀 ○ 가끔 ○ 자주          │ │  ← 재사용 (P/C 동일)
│ └──────────────────────────────────────────────┘ │
│ [SurveyNavigation]  [이전] [다음] / [제출]          │  ← 버튼 3종 묶음
└──────────────────────────────────────────────────┘
+ [SecondExamDialog] — KDSQ-P 4점 이상일 때 2차 검사 안내 모달
```

**④ SCR-05 검사 결과**
```
┌──────────────────────────────────────────────────┐
│ [ResultHeader]  검사 결과                          │
│ [ResultStatus]  정상 / 주의 / 고위험군 (Badge+Alert) │
│ [TotalScore]    총점 5점 / 30점                     │
│ [ScoreSummary]  기억력 / 기타인지 / ADL (KDSQ-C만)   │  ← 조건부
│ [ExamDate]      검사일시                            │
│ [Recommendation] 관리·추가 검진 안내 (KDSQ-C만)      │  ← 조건부
│ [HistoryButton] 검사 이력 보기                      │
└──────────────────────────────────────────────────┘
```

**⑤ SCR-06 마이페이지**
```
┌──────────────────────────────────────────────────┐
│ [ProfileCard] 회원정보 + [ReadOnlyNotice]          │  ← 조회 전용
│ [ExamHistoryTable] 검사일/검사/총점/판정/[보기]      │
│      └ [RiskBadge]                                │  ← 결과 화면과 공유 → 공통
│ [ExamTrendChart] 점수 추이 그래프                   │
│   또는 [EmptyHistory] (이력 0건)                   │
└──────────────────────────────────────────────────┘
```

---

### STEP 2 — 컴포넌트 트리(계층 구조)

```
App                                        ← 라우터 설정
  └── UserLayout                           ← Header + Outlet + Footer
        ├── Header                         ← (State: authStore.token) 네비게이션, 로그아웃
        ├── LoginPage          /login      ← (State: loading, errorMessage) 로그인 처리
        │     └── LoginForm                ← (Props: onSubmit, loading, errorMessage)
        ├── RegisterPage       /register   ← (State: loading, serverError) 가입 처리
        │     └── RegisterForm             ← (Props: onSubmit, loading, serverError)
        ├── ProtectedRoute                 ← (State: authStore.token) 비로그인 → /login
        │     ├── SurveyPage   /surveys    ← (Props: type="P") (State: currentIndex, answers …)
        │     │     ├── SurveyHeader       ← (Props: type)
        │     │     ├── ProgressBar        ← (Props: current, total)
        │     │     ├── QuestionCard       ← (Props: question, number, error)
        │     │     │     └── AnswerOptions← (Props: options, value, onChange)
        │     │     ├── SurveyNavigation   ← (Props: isFirst, isLast, submitting, onPrev, onNext, onSubmit)
        │     │     └── SecondExamDialog   ← (Props: open, firstScore, onConfirm, onLater)
        │     ├── SurveyPage   /surveys/c  ← (Props: type="C") 위와 동일 구성 (Dialog 제외)
        │     ├── ResultPage   /results/:examId ← (State: examStore.currentExam)
        │     │     ├── ResultHeader
        │     │     ├── ResultStatus       ← (Props: exam)
        │     │     ├── TotalScore         ← (Props: score, maxScore)
        │     │     ├── ScoreSummary       ← (Props: memoryScore, otherScore, adlScore)
        │     │     ├── ExamDate           ← (Props: date)
        │     │     ├── Recommendation     ← (Props: riskLevel)
        │     │     └── HistoryButton      ← (Props: onClick)
        │     └── MyPage       /mypage     ← (State: memberStore.me, examStore.history)
        │           ├── ProfileCard        ← (Props: member)
        │           │     └── ReadOnlyNotice
        │           ├── ExamHistoryTable   ← (Props: exams, onDetail)
        │           │     └── RiskBadge    ← (Props: exam)
        │           ├── ExamTrendChart     ← (Props: exams)
        │           └── EmptyHistory       ← (Props: onStart)
        └── Footer
```

---

### STEP 3 — 상태(State) 위치 결정

| 상태 | 위치 | Q1 공유? | Q2 영속? | Q3 서버? | 결정 이유 |
|------|------|:---:|:---:|:---:|-----------|
| `token`, `email` | `authStore` (persist: localStorage) | O | O | O | Header·ProtectedRoute·axios 인터셉터 등 앱 전체에서 인증 확인, 새로고침 후 유지 |
| `questions` (문항) | `surveyStore` | △ | X | O | 서버 데이터, P/C 타입별로 fetch |
| `currentExam` | `examStore` | O | X | O | 제출 직후 결과 화면에서 재사용 (재조회 최소화) |
| `history` (검사 이력) | `examStore` | O | X | O | 마이페이지 테이블·차트가 공유 |
| `pendingPExamId` | `examStore` (persist: sessionStorage) | O | O | X | KDSQ-P 4점 이상 → `/surveys/c` 진입 자격. 새로고침해도 2차 검사 진행 가능해야 함 |
| `me` (회원정보) | `memberStore` | △ | X | O | 서버 데이터, 추후 Header 이름 표시 등 확장 대비 |
| `currentIndex` (현재 문항) | `SurveyPage` 로컬 | X | X | X | 검사 화면 안에서만 사용 |
| `answers` (응답) | `SurveyPage` 로컬 | X | X | X | 문항별 응답은 **저장하지 않음**(UI 설계서 3.4.4) → 제출 시 점수로 환산 후 폐기 |
| `validationError` | `SurveyPage` 로컬 | X | X | X | "답변을 선택해주세요." UI 상태 |
| `isSecondDialogOpen` | `SurveyPage` 로컬 | X | X | X | 모달 열림/닫힘 |
| 로그인/회원가입 폼 입력값 | `LoginForm` / `RegisterForm` 로컬 | X | X | X | 폼 내부에서만 사용 |
| `loading`, `errorMessage` (로그인·가입) | `LoginPage` / `RegisterPage` 로컬 | X | X | X | 해당 페이지 제출 상태 |

---

### STEP 4 — 문서 작성
STEP 1~3 결과를 아래 섹션 1~8에 정리합니다.

---

## 1. 컴포넌트 개요

### 1.1 컴포넌트 목록

| 화면 ID | 페이지 컴포넌트 | URL | 하위 컴포넌트 |
|---|---|---|---|
| (공통) | `UserLayout` | — | `Header`, `Footer` |
| SCR-01 | `LoginPage` | `/login` | `LoginForm` |
| SCR-02 | `RegisterPage` | `/register` | `RegisterForm` |
| SCR-03 | `SurveyPage` (`type="P"`) | `/surveys` | `SurveyHeader`, `ProgressBar`, `QuestionCard`, `AnswerOptions`, `SurveyNavigation`, `SecondExamDialog` |
| SCR-04 | `SurveyPage` (`type="C"`) | `/surveys/c` | SCR-03과 동일 (Dialog 제외) |
| SCR-05 | `ResultPage` | `/results/:examId` | `ResultHeader`, `ResultStatus`, `TotalScore`, `ScoreSummary`, `ExamDate`, `Recommendation`, `HistoryButton` |
| SCR-06 | `MyPage` | `/mypage` | `ProfileCard`, `ReadOnlyNotice`, `ExamHistoryTable`, `RiskBadge`, `ExamTrendChart`, `EmptyHistory` |

### 1.2 목적 및 역할

| 컴포넌트 | 주요 목적 | 위치 | 유형 |
|---|---|---|---|
| `UserLayout` | Header/Footer 공통 배치, `<Outlet />` 렌더링 | `src/components/layout/UserLayout.jsx` | 레이아웃 |
| `ProtectedRoute` | 토큰 없으면 `/login`으로 리다이렉트 | `src/components/common/ProtectedRoute.jsx` | 공통 |
| `LoginPage` | 로그인 API 호출, 성공 시 `/surveys` 이동 | `src/pages/Login/index.jsx` | 페이지 |
| `RegisterPage` | 회원가입 API 호출, 성공 시 `/login` 이동 | `src/pages/Register/index.jsx` | 페이지 |
| `SurveyPage` | P/C 문항 진행, 점수 산출, 제출 및 분기 | `src/pages/Survey/index.jsx` | 페이지 |
| `ResultPage` | 검사 점수·판정·관리 안내 표시 | `src/pages/Result/index.jsx` | 페이지 |
| `MyPage` | 회원정보(조회 전용), 검사 이력, 추이 | `src/pages/MyPage/index.jsx` | 페이지 |

> **설계 포인트 — SCR-03/04 통합**: 두 화면은 와이어프레임·컴포넌트 매핑·상태가 동일하고 문항 수(5/15)와 제출 API만 다릅니다. 따라서 `SurveyPage`에 `type` Props(`'P' | 'C'`)를 주어 재사용합니다.

### 1.3 기능 요구사항

**SCR-01 로그인 (`LoginPage`)**
- [ ] 이메일/비밀번호 입력 및 필수값·이메일 형식 검증
- [ ] 로그인 진행 중 버튼 비활성화 + 로딩
- [ ] 401 시 "이메일 또는 비밀번호를 확인해주세요." (계정 존재 여부 구분 노출 금지)
- [ ] 성공 시 토큰 저장 후 `/surveys` 이동
- [ ] 회원가입 링크 → `/register`

**SCR-02 회원가입 (`RegisterPage`)**
- [ ] 이름(1~50자) / 이메일(형식, 최대 100자) / 비밀번호 / 비밀번호 확인 / 성별 / 출생년도(1900~현재) 검증
- [ ] 필드별 오류 메시지 표시
- [ ] 409 시 "이미 가입된 이메일입니다."
- [ ] 성공 시 가입 완료 토스트 후 `/login` 이동

**SCR-03/04 검사 (`SurveyPage`)**
- [ ] 진입 시 문항 조회 (`GET /api/surveys/p` 또는 `/c`)
- [ ] 한 문항씩 표시 + 진행률
- [ ] 이전/다음 이동, 미선택 시 "답변을 선택해주세요."
- [ ] 이전 문항으로 돌아가도 기존 응답 유지
- [ ] 마지막 문항에서 제출, 제출 중 버튼 비활성화
- [ ] P: `firstScore` 0~3 → `/results/:examId`, 4~10 → 2차 안내 모달 → `/surveys/c`
- [ ] C: `memoryScore`/`otherScore`/`adlScore` 산출·제출 → `/results/:examId`
- [ ] `/surveys/c` 직접 접근 시 2차 대상이 아니면 `/surveys`로 리다이렉트

**SCR-05 결과 (`ResultPage`)**
- [ ] `GET /api/exams/:examId` 조회 (제출 직후면 store 값 재사용)
- [ ] KDSQ-P: 총점(/10) + 정상 문구
- [ ] KDSQ-C: 영역별 점수 + 총점(/30, 프론트 계산) + 주의/고위험군 + 관리 안내
- [ ] 조회 실패 시 오류 + 재시도
- [ ] [검사 이력 보기] → `/mypage`

**SCR-06 마이페이지 (`MyPage`)**
- [ ] 회원정보 조회 전용 표시 + 수정 불가 안내
- [ ] 검사 이력 테이블 (최신순) + [보기] → `/results/:examId`
- [ ] 점수 추이 그래프 (날짜 오름차순)
- [ ] 이력 0건 → "아직 검사 이력이 없습니다." + 검사 시작 버튼
- [ ] 로딩 / 오류 + 재시도

---

## 2. 컴포넌트 구조 설계

### 2.1 컴포넌트 분류

| 컴포넌트 | 타입 | 복잡도 | 상태 관리 |
|---|---|---|---|
| `UserLayout` | 레이아웃 | 단순 | 상태 없음 |
| `Header` | 레이아웃 | 단순 | Zustand 연결 (`authStore`) |
| `Footer` | 레이아웃 | 단순 | 상태 없음 |
| `ProtectedRoute` | 공통 | 단순 | Zustand 연결 (`authStore`) |
| `LoadingSpinner` / `ErrorAlert` / `RiskBadge` | 공통 | 단순 | 상태 없음 |
| `LoginPage` | 페이지 | 단순 | 로컬 + Zustand (`authStore`) |
| `LoginForm` | 기능 | 보통 | 로컬 상태만 |
| `RegisterPage` | 페이지 | 단순 | 로컬 상태만 |
| `RegisterForm` | 기능 | 복합 | 로컬 상태만 |
| `SurveyPage` | 페이지 | 복합 | 로컬 + Zustand (`surveyStore`, `examStore`) |
| `SurveyHeader` / `ProgressBar` / `QuestionCard` / `AnswerOptions` / `SurveyNavigation` | 기능 | 단순 | 상태 없음 |
| `SecondExamDialog` | 기능 | 단순 | 상태 없음 |
| `ResultPage` | 페이지 | 보통 | Zustand (`examStore`) |
| `ResultStatus` / `TotalScore` / `ScoreSummary` / `ExamDate` / `Recommendation` | 기능 | 단순 | 상태 없음 |
| `MyPage` | 페이지 | 보통 | Zustand (`memberStore`, `examStore`) |
| `ProfileCard` / `ExamHistoryTable` / `ExamTrendChart` / `EmptyHistory` | 기능 | 단순~보통 | 상태 없음 |

### 2.2 컴포넌트 트리 및 책임

트리는 [STEP 2](#step-2--컴포넌트-트리계층-구조) 참고.

**컴포넌트별 책임 요약**
| 컴포넌트명 | 파일 위치 | 주요 책임 | 재사용성 |
|---|---|---|---|
| `UserLayout` | `components/layout/UserLayout.jsx` | Header/Footer + Outlet | 높음 |
| `Header` | `components/layout/Header.jsx` | 로고·메뉴, 로그인 상태별 버튼, 로그아웃 | 높음 |
| `Footer` | `components/layout/Footer.jsx` | 저작권 표기 | 높음 |
| `ProtectedRoute` | `components/common/ProtectedRoute.jsx` | 인증 가드 | 높음 |
| `LoadingSpinner` | `components/common/LoadingSpinner.jsx` | 로딩 표시 | 높음 |
| `ErrorAlert` | `components/common/ErrorAlert.jsx` | 오류 문구 + 재시도 버튼 | 높음 |
| `RiskBadge` | `components/common/RiskBadge.jsx` | 정상/주의/위험/2차대상 배지 | 높음 (결과·마이페이지 공유) |
| `LoginPage` | `pages/Login/index.jsx` | 로그인 API, 이동 | 낮음 |
| `LoginForm` | `pages/Login/components/LoginForm.jsx` | 입력·검증·제출 | 보통 |
| `RegisterPage` | `pages/Register/index.jsx` | 가입 API, 이동 | 낮음 |
| `RegisterForm` | `pages/Register/components/RegisterForm.jsx` | 입력·필드별 검증·제출 | 보통 |
| `SurveyPage` | `pages/Survey/index.jsx` | 문항 진행 상태, 점수 산출, 제출·분기 | 보통 (P/C 공용) |
| `SurveyHeader` | `pages/Survey/components/SurveyHeader.jsx` | 검사명·안내 | 보통 |
| `ProgressBar` | `pages/Survey/components/ProgressBar.jsx` | 진행률 | 보통 |
| `QuestionCard` | `pages/Survey/components/QuestionCard.jsx` | 문항 표시 | 보통 |
| `AnswerOptions` | `pages/Survey/components/AnswerOptions.jsx` | 선택지 RadioGroup | 보통 |
| `SurveyNavigation` | `pages/Survey/components/SurveyNavigation.jsx` | 이전/다음/제출 버튼 | 보통 |
| `SecondExamDialog` | `pages/Survey/components/SecondExamDialog.jsx` | 2차 검사 안내 모달 | 낮음 |
| `ResultPage` | `pages/Result/index.jsx` | 결과 조회·분기 렌더링 | 낮음 |
| `ResultHeader` 외 6종 | `pages/Result/components/` | 결과 표시 단위 | 보통 |
| `MyPage` | `pages/MyPage/index.jsx` | 회원정보·이력 조회 | 낮음 |
| `ProfileCard` 외 4종 | `pages/MyPage/components/` | 마이페이지 표시 단위 | 보통 |

**UI 설계서 컴포넌트 ↔ React 컴포넌트 매핑**
| UI 설계서 표기 | React 설계 | 비고 |
|---|---|---|
| `PreviousButton` / `NextButton` / `SubmitButton` | `SurveyNavigation` | 세 버튼이 항상 같은 영역에서 함께 바뀌므로 하나로 묶음 |
| `Alert` (오류) | `ErrorAlert` | shadcn `Alert` 래핑 + 재시도 버튼 |
| `Alert` (로그인/가입 필드 오류) | 폼 내부 helperText | 필드 단위 표시 |
| KDSQ-C "안내 후 이동" | `SecondExamDialog` | shadcn `Dialog` |
| `ReadOnlyNotice` | `ProfileCard` 내부 | 별도 파일 없이 ProfileCard 안에 포함 가능 |

### 2.3 파일 구조
```
src/
├── App.jsx                            # 라우터 설정
├── main.jsx
├── pages/
│   ├── Login/
│   │   ├── index.jsx                  # LoginPage
│   │   └── components/
│   │       └── LoginForm.jsx
│   ├── Register/
│   │   ├── index.jsx                  # RegisterPage
│   │   └── components/
│   │       └── RegisterForm.jsx
│   ├── Survey/
│   │   ├── index.jsx                  # SurveyPage (type: 'P' | 'C')
│   │   └── components/
│   │       ├── SurveyHeader.jsx
│   │       ├── ProgressBar.jsx
│   │       ├── QuestionCard.jsx
│   │       ├── AnswerOptions.jsx
│   │       ├── SurveyNavigation.jsx
│   │       └── SecondExamDialog.jsx
│   ├── Result/
│   │   ├── index.jsx                  # ResultPage
│   │   └── components/
│   │       ├── ResultHeader.jsx
│   │       ├── ResultStatus.jsx
│   │       ├── TotalScore.jsx
│   │       ├── ScoreSummary.jsx
│   │       ├── ExamDate.jsx
│   │       ├── Recommendation.jsx
│   │       └── HistoryButton.jsx
│   └── MyPage/
│       ├── index.jsx                  # MyPage
│       └── components/
│           ├── ProfileCard.jsx
│           ├── ExamHistoryTable.jsx
│           ├── ExamTrendChart.jsx
│           └── EmptyHistory.jsx
├── components/
│   ├── layout/
│   │   ├── UserLayout.jsx
│   │   ├── Header.jsx
│   │   └── Footer.jsx
│   ├── common/
│   │   ├── ProtectedRoute.jsx
│   │   ├── LoadingSpinner.jsx
│   │   ├── ErrorAlert.jsx
│   │   └── RiskBadge.jsx
│   └── ui/                            # shadcn/ui 생성 컴포넌트
├── store/
│   ├── authStore.js                   # 인증 (persist: localStorage)
│   ├── surveyStore.js                 # 문항
│   ├── examStore.js                   # 검사 제출·결과·이력 (pendingPExamId: sessionStorage)
│   └── memberStore.js                 # 내 회원정보
├── api/
│   ├── client.js                      # axios 인스턴스 + 인터셉터
│   ├── authApi.js
│   ├── surveyApi.js
│   ├── examApi.js
│   └── memberApi.js
├── constants/
│   └── kdsq.js                        # 판정 기준, 선택지, 표시 라벨
└── utils/
    ├── kdsq.js                        # 점수 계산·판정 함수
    └── format.js                      # 날짜/성별/검사종류 포맷
```

### 2.4 라우팅 구성
```jsx
<Routes>
  <Route element={<UserLayout />}>
    <Route path="/login" element={<LoginPage />} />
    <Route path="/register" element={<RegisterPage />} />
    <Route element={<ProtectedRoute />}>
      <Route path="/surveys" element={<SurveyPage type="P" key="P" />} />
      <Route path="/surveys/c" element={<SurveyPage type="C" key="C" />} />
      <Route path="/results/:examId" element={<ResultPage />} />
      <Route path="/mypage" element={<MyPage />} />
    </Route>
    <Route path="*" element={<Navigate to="/login" replace />} />
  </Route>
</Routes>
```
> `key`를 다르게 주어 P → C 이동 시 `SurveyPage`의 로컬 상태(`currentIndex`, `answers`)가 초기화되도록 합니다.

---

## 3. Props 설계

### 3.1 도메인 타입 정의 (공통 참조)

```typescript
// 회원
type Member = {
  id: number;
  name: string;
  email: string;
  gender: 'MALE' | 'FEMALE';
  birthYear: number;
  createdAt: string;            // ISO datetime
};

// 문항
type Question = {
  id: number;
  order: number;                // 1부터 시작. C는 1~5 기억력 / 6~10 기타인지 / 11~15 ADL
  content: string;              // 문항 내용
};

// 선택지 (constants/kdsq.js 고정값)
type AnswerOption = { label: string; value: 0 | 1 | 2 };
// [{ label:'전혀 그렇지 않다', value:0 }, { label:'가끔 그렇다', value:1 }, { label:'자주 그렇다', value:2 }]

// 검사 결과
type Exam = {
  examId: number;
  examType: 'KDSQ_P' | 'KDSQ_C';
  firstScore: number | null;    // KDSQ_P (0~10)
  memoryScore: number | null;   // KDSQ_C (0~10)
  otherScore: number | null;    // KDSQ_C (0~10)
  adlScore: number | null;      // KDSQ_C (0~10)
  riskLevel: 'Normal' | 'Borderline' | 'HighRisk' | null;
  createdAt: string;
};
// totalScore는 저장하지 않음 → utils/kdsq.js getTotalScore(exam)로 프론트 계산
```

### 3.2 공통 컴포넌트 Props

**`LoadingSpinner`**
| Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|
| `message` | `string` | 선택 | `'불러오는 중...'` | 스피너 아래 문구 |
| `fullScreen` | `boolean` | 선택 | `false` | 화면 중앙 전체 영역 표시 여부 |

**`ErrorAlert`**
| Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|
| `message` | `string` | 필수 | — | 오류 문구 |
| `onRetry` | `() => void` | 선택 | — | 전달 시 [다시 시도] 버튼 표시 |

**`RiskBadge`**
| Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|
| `exam` | `Exam` | 필수 | — | `getRiskLabel(exam)`으로 라벨·색상 결정 (정상=Success, 주의=Warning, 위험=Danger, 2차대상=Neutral) |

> `Header`, `Footer`, `UserLayout`, `ProtectedRoute`는 Props 없음 (`authStore` 직접 구독 / `<Outlet />` 사용).

### 3.3 SCR-01 로그인

**`LoginForm`**
| Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|
| `onSubmit` | `(email: string, password: string) => void` | 필수 | — | 클라이언트 검증 통과 후 호출 |
| `loading` | `boolean` | 선택 | `false` | 버튼 비활성화 + 로딩 표시 |
| `errorMessage` | `string \| null` | 선택 | `null` | 서버 오류 문구 (Alert) |

### 3.4 SCR-02 회원가입

**`RegisterForm`**
| Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|
| `onSubmit` | `(form: { name, email, password, gender, birthYear }) => void` | 필수 | — | 검증 통과 후 호출 (`passwordConfirm` 제외) |
| `loading` | `boolean` | 선택 | `false` | 제출 중 버튼 비활성화 |
| `serverError` | `string \| null` | 선택 | `null` | 409/400 등 서버 오류 문구 |

### 3.5 SCR-03/04 검사

**`SurveyPage`**
| Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|
| `type` | `'P' \| 'C'` | 필수 | — | 검사 종류. 문항 API·제출 API·분기 로직 결정 |

**`SurveyHeader`**
| Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|
| `type` | `'P' \| 'C'` | 필수 | — | P: "KDSQ-P 인지선별검사 (1차)", C: "KDSQ-C 상세검사" |

**`ProgressBar`**
| Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|
| `current` | `number` | 필수 | — | 현재 문항 번호 (1부터) |
| `total` | `number` | 필수 | — | 전체 문항 수 (5 / 15) |

**`QuestionCard`**
| Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|
| `question` | `Question` | 필수 | — | 현재 문항 |
| `number` | `number` | 필수 | — | 표시 번호 (Q7) |
| `error` | `string` | 선택 | `''` | 미선택 오류 문구 |
| `children` | `ReactNode` | 필수 | — | `AnswerOptions` 삽입 |

**`AnswerOptions`**
| Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|
| `options` | `AnswerOption[]` | 선택 | `ANSWER_OPTIONS` | 선택지 목록 |
| `value` | `number \| null` | 필수 | — | 현재 선택값 (이전 문항 복귀 시 복원) |
| `onChange` | `(value: number) => void` | 필수 | — | 선택 시 호출 |
| `name` | `string` | 필수 | — | RadioGroup name (문항 id 기반) |

**`SurveyNavigation`**
| Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|
| `isFirst` | `boolean` | 필수 | — | true면 [이전] 비활성화 |
| `isLast` | `boolean` | 필수 | — | true면 [다음] 대신 [제출] 표시 |
| `submitting` | `boolean` | 선택 | `false` | 제출 버튼 비활성화 + 로딩 |
| `onPrev` | `() => void` | 필수 | — | 이전 문항 이동 |
| `onNext` | `() => void` | 필수 | — | 다음 문항 이동 (미선택 검증 포함) |
| `onSubmit` | `() => void` | 필수 | — | 최종 제출 |

**`SecondExamDialog`**
| Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|
| `open` | `boolean` | 필수 | — | 모달 열림 여부 |
| `firstScore` | `number` | 필수 | — | 안내 문구에 1차 점수 표시 |
| `onConfirm` | `() => void` | 필수 | — | [2차 검사 시작] → `/surveys/c` |
| `onLater` | `() => void` | 선택 | — | [나중에 하기] → `/mypage` (※ 정책 확인 필요, 6.4 참고) |

### 3.6 SCR-05 결과

| 컴포넌트 | Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|---|
| `ResultStatus` | `exam` | `Exam` | 필수 | — | 판정 문구·색상 결정 |
| `TotalScore` | `score` | `number` | 필수 | — | 총점 |
| | `maxScore` | `10 \| 30` | 필수 | — | P=10, C=30 |
| `ScoreSummary` | `memoryScore` | `number` | 필수 | — | 기억력 (/10) |
| | `otherScore` | `number` | 필수 | — | 기타 인지기능 (/10) |
| | `adlScore` | `number` | 필수 | — | 일상생활수행능력 (/10) |
| `ExamDate` | `date` | `string` | 필수 | — | `createdAt` → `YYYY-MM-DD` 포맷 |
| `Recommendation` | `riskLevel` | `'Borderline' \| 'HighRisk'` | 필수 | — | 판정별 관리·추가 검진 안내 문구 |
| `HistoryButton` | `onClick` | `() => void` | 필수 | — | `/mypage` 이동 |

> `ResultHeader`는 Props 없음. `ScoreSummary`, `Recommendation`은 **KDSQ-C일 때만** 렌더링.

### 3.7 SCR-06 마이페이지

| 컴포넌트 | Props명 | 타입 | 필수여부 | 기본값 | 설명 |
|---|---|---|---|---|---|
| `ProfileCard` | `member` | `Member` | 필수 | — | 이름/이메일/성별/출생년도 + 수정 불가 안내 |
| `ExamHistoryTable` | `exams` | `Exam[]` | 필수 | — | 최신순 정렬된 이력 |
| | `onDetail` | `(examId: number) => void` | 필수 | — | [보기] 클릭 → `/results/:examId` |
| `ExamTrendChart` | `exams` | `Exam[]` | 필수 | — | 날짜 오름차순 차트 데이터 (P·C 별도 라인) |
| `EmptyHistory` | `onStart` | `() => void` | 필수 | — | [검사 시작하기] → `/surveys` |

> **차트 설계**: KDSQ-P(10점 만점)와 KDSQ-C(30점 만점)는 척도가 달라 한 라인으로 이으면 추이가 왜곡됩니다. 검사 종류별로 라인 2개(`KDSQ-P 총점`, `KDSQ-C 총점`)로 분리해 표시합니다.

---

## 4. 상태 관리 설계

### 4.1 상태 위치 결정 근거
[STEP 3](#step-3--상태state-위치-결정) 표 참고.

### 4.2 로컬 상태 (useState)

**`LoginForm`**
| 상태명 | 타입 | 초기값 | 용도 |
|---|---|---|---|
| `email` | `string` | `''` | 이메일 입력값 |
| `password` | `string` | `''` | 비밀번호 입력값 |
| `fieldErrors` | `{ email?: string; password?: string }` | `{}` | 필수값·형식 오류 |

**`LoginPage`**
| 상태명 | 타입 | 초기값 | 용도 |
|---|---|---|---|
| `loading` | `boolean` | `false` | 로그인 진행 중 |
| `errorMessage` | `string \| null` | `null` | 401/서버 오류 문구 |

**`RegisterForm`**
| 상태명 | 타입 | 초기값 | 용도 |
|---|---|---|---|
| `form` | `{ name, email, password, passwordConfirm, gender, birthYear }` | 모두 `''` | 입력값 |
| `fieldErrors` | `Record<string, string>` | `{}` | 필드별 검증 오류 |

**`RegisterPage`**
| 상태명 | 타입 | 초기값 | 용도 |
|---|---|---|---|
| `loading` | `boolean` | `false` | 가입 진행 중 |
| `serverError` | `string \| null` | `null` | 409 "이미 가입된 이메일입니다." 등 |

**`SurveyPage`**
| 상태명 | 타입 | 초기값 | 용도 |
|---|---|---|---|
| `currentIndex` | `number` | `0` | 현재 문항 인덱스 |
| `answers` | `(number \| null)[]` | `[]` (문항 로드 후 `Array(n).fill(null)`) | 문항별 응답값 (제출 후 폐기) |
| `validationError` | `string` | `''` | "답변을 선택해주세요." |
| `isSecondDialogOpen` | `boolean` | `false` | 2차 안내 모달 |
| `pFirstScore` | `number \| null` | `null` | 모달에 표시할 1차 점수 |

### 4.3 Zustand 전역 상태

**[x] authStore 사용** — `persist` (localStorage, key: `kdsq-auth`)
| 상태/액션 | 타입 | 사용 컴포넌트 · 용도 |
|---|---|---|
| `token` | `string \| null` | `ProtectedRoute` 접근 제어, `Header` 버튼 분기, axios 인터셉터 |
| `email` | `string \| null` | 로그인 사용자 식별 |
| `login(email, password)` | `async function` | `LoginPage` 폼 제출 시 |
| `logout()` | `async function` | `Header` [로그아웃], 401 인터셉터 — `POST /api/auth/logout` 후 상태·다른 스토어 초기화 |

**[x] surveyStore 사용**
| 상태/액션 | 타입 | 사용 컴포넌트 · 용도 |
|---|---|---|
| `questions` | `Question[]` | `SurveyPage` 문항 목록 |
| `loading` | `boolean` | 문항 로딩 표시 |
| `error` | `string \| null` | 조회 실패 표시 |
| `fetchQuestions(type)` | `async function` | `SurveyPage` 마운트 시 |

**[x] examStore 사용** — `pendingPExamId`만 `persist` (sessionStorage, `partialize`)
| 상태/액션 | 타입 | 사용 컴포넌트 · 용도 |
|---|---|---|
| `currentExam` | `Exam \| null` | `ResultPage` 표시 (제출 직후 재사용) |
| `history` | `Exam[]` | `MyPage` 테이블·차트 |
| `pendingPExamId` | `number \| null` | KDSQ-P 4점 이상 시 저장 → `/surveys/c` 진입 가드·C 제출 시 연결 |
| `submitting` | `boolean` | 제출 버튼 비활성화 |
| `loading` / `error` | `boolean` / `string \| null` | 결과·이력 조회 상태 |
| `submitP(firstScore)` | `async function → Exam` | P 제출. 4점 이상이면 `pendingPExamId` 저장 |
| `submitC(scores)` | `async function → Exam` | C 제출. 성공 시 `pendingPExamId` 초기화 |
| `fetchExam(examId)` | `async function` | `ResultPage` 진입 시 (store에 같은 id 있으면 생략) |
| `fetchHistory()` | `async function` | `MyPage` 진입 시 |
| `reset()` | `function` | 로그아웃 시 초기화 |

**[x] memberStore 사용**
| 상태/액션 | 타입 | 사용 컴포넌트 · 용도 |
|---|---|---|
| `me` | `Member \| null` | `ProfileCard` |
| `loading` / `error` | `boolean` / `string \| null` | 회원정보 조회 상태 |
| `fetchMe()` | `async function` | `MyPage` 진입 시 |
| `reset()` | `function` | 로그아웃 시 초기화 |

### 4.4 점수·판정 유틸 (`utils/kdsq.js`, `constants/kdsq.js`)

컴포넌트마다 판정 로직이 흩어지지 않도록 한 곳에 모읍니다. (UI 설계서 1.4 판정 기준)

```javascript
// constants/kdsq.js
export const KDSQ_P_SECOND_THRESHOLD = 4;   // P 4~10 → 2차 대상
export const KDSQ_C_HIGH_RISK_THRESHOLD = 6; // C 6~30 → 고위험군
export const MAX_SCORE = { KDSQ_P: 10, KDSQ_C: 30 };
export const ANSWER_OPTIONS = [
  { label: '전혀 그렇지 않다', value: 0 },
  { label: '가끔 그렇다', value: 1 },
  { label: '자주 그렇다', value: 2 },
];
export const RISK_LABEL = { Normal: '정상', Borderline: '주의', HighRisk: '위험' };
export const EXAM_TYPE_LABEL = { KDSQ_P: 'KDSQ-P', KDSQ_C: 'KDSQ-C' };
export const GENDER_LABEL = { MALE: '남성', FEMALE: '여성' };

// utils/kdsq.js
const sum = (arr) => arr.reduce((a, b) => a + b, 0);

export const calcFirstScore = (answers) => sum(answers);            // 0~10

export const calcCScores = (answers) => ({                          // 각 0~10
  memoryScore: sum(answers.slice(0, 5)),   // Q1~5
  otherScore:  sum(answers.slice(5, 10)),  // Q6~10
  adlScore:    sum(answers.slice(10, 15)), // Q11~15
});

export const getTotalScore = (exam) =>
  exam.examType === 'KDSQ_P'
    ? exam.firstScore
    : exam.memoryScore + exam.otherScore + exam.adlScore;           // 프론트 계산

export const getRiskLabel = (exam) => {
  if (exam.examType === 'KDSQ_P') {
    return exam.firstScore >= KDSQ_P_SECOND_THRESHOLD ? '2차 대상' : '정상';
  }
  return getTotalScore(exam) >= KDSQ_C_HIGH_RISK_THRESHOLD ? '고위험군' : '주의';
};
```
> 서버의 `riskLevel`이 있으면 표시에 우선 사용하고, 위 함수는 `riskLevel`이 없는 경우(P 2차 대상 등)의 보조 판정으로 사용합니다.

---

## 5. API 연동 설계

### 5.1 사용하는 API 목록
| API 엔드포인트 | 메서드 | 용도 | 호출 시점 | 호출 위치 | 인증 |
|---|---|---|---|---|---|
| `/api/auth/login` | `POST` | 로그인 | 로그인 버튼 클릭 | `authStore.login` | 불필요 |
| `/api/auth/register` | `POST` | 회원가입 | 회원가입 버튼 클릭 | `RegisterPage` → `authApi.register` | 불필요 |
| `/api/auth/logout` | `POST` | 로그아웃(토큰 폐기) | Header [로그아웃] | `authStore.logout` | 필요 |
| `/api/surveys/p` | `GET` | KDSQ-P 문항 조회 | `/surveys` 진입 | `surveyStore.fetchQuestions('P')` | 필요 |
| `/api/surveys/c` | `GET` | KDSQ-C 문항 조회 | `/surveys/c` 진입 | `surveyStore.fetchQuestions('C')` | 필요 |
| `/api/exams/p` | `POST` | KDSQ-P 결과 저장 | 5번 문항 제출 | `examStore.submitP` | 필요 |
| `/api/exams/c` | `POST` | KDSQ-C 결과 저장 | 15번 문항 제출 | `examStore.submitC` | 필요 |
| `/api/exams/:examId` | `GET` | 검사 결과 조회 | `/results/:examId` 진입 | `examStore.fetchExam` | 필요 |
| `/api/exams/history` | `GET` | 내 검사 이력 | `/mypage` 진입 | `examStore.fetchHistory` | 필요 |
| `/api/members/me` | `GET` | 내 회원정보 | `/mypage` 진입 | `memberStore.fetchMe` | 필요 |

> 인증 필요 API는 `api/client.js`의 요청 인터셉터가 `Authorization: Bearer {token}`을 자동 첨부합니다.

### 5.2 Request / Response 예시
> 아래 형식은 공통 응답 래퍼(`success`, `data`)를 가정한 예시입니다. **백엔드 API 명세서와 필드명을 최종 대조**해 주세요.

```javascript
// POST /api/auth/login
// Request
{ "email": "hong@example.com", "password": "password123" }
// Response 200
{ "success": true, "data": { "accessToken": "eyJhbGciOi...", "email": "hong@example.com" } }

// POST /api/auth/register
// Request
{ "name": "홍길동", "email": "hong@example.com", "password": "password123",
  "gender": "MALE", "birthYear": 1960 }
// Response 200
{ "success": true, "data": { "id": 1 } }
// Response 409
{ "success": false, "message": "이미 가입된 이메일입니다." }

// GET /api/surveys/c
// Response 200
{ "success": true, "data": [
  { "id": 6, "order": 1, "content": "오늘이 몇 월이고 무슨 요일인지 잘 모른다." },
  // ... 15문항
] }

// POST /api/exams/p
// Request — 문항별 answers는 전송·저장하지 않음 (UI 설계서 3.4.4)
{ "firstScore": 6 }
// Response 200
{ "success": true, "data": { "examId": 101, "examType": "KDSQ_P", "firstScore": 6,
  "riskLevel": null, "createdAt": "2026-09-22T10:15:00" } }

// POST /api/exams/c
// Request
{ "pExamId": 101, "memoryScore": 2, "otherScore": 2, "adlScore": 1 }
// Response 200
{ "success": true, "data": { "examId": 102, "examType": "KDSQ_C", "memoryScore": 2,
  "otherScore": 2, "adlScore": 1, "riskLevel": "Borderline",
  "createdAt": "2026-09-22T10:20:00" } }

// GET /api/exams/history
// Response 200
{ "success": true, "data": [
  { "examId": 102, "examType": "KDSQ_C", "firstScore": null, "memoryScore": 2,
    "otherScore": 2, "adlScore": 1, "riskLevel": "Borderline", "createdAt": "2026-09-22T10:20:00" },
  { "examId": 55, "examType": "KDSQ_P", "firstScore": 3, "memoryScore": null,
    "otherScore": null, "adlScore": null, "riskLevel": "Normal", "createdAt": "2026-03-10T09:00:00" }
] }

// GET /api/members/me
// Response 200
{ "success": true, "data": { "id": 1, "name": "홍길동", "email": "hong@example.com",
  "gender": "MALE", "birthYear": 1960, "createdAt": "2026-01-10T12:00:00" } }
```

### 5.3 에러 처리 방식
| HTTP 상태 | 의미 | 처리 방식 | 처리 위치 |
|---|---|---|---|
| `400` | 유효성 오류 | 폼/필드 오류 메시지 표시 | 각 페이지 |
| `401` (로그인 API) | 인증 실패 | "이메일 또는 비밀번호를 확인해주세요." | `LoginPage` |
| `401` (그 외) | 토큰 만료·무효 | `logout()` → `/login` 이동 + "다시 로그인해주세요." 토스트 | axios 응답 인터셉터 |
| `403` | 권한 없음 (타인 결과 조회 등) | "접근 권한이 없습니다" 토스트 → `/mypage` | `ResultPage` |
| `404` | 리소스 없음 | "검사 결과를 찾을 수 없습니다." + [검사 이력 보기] | `ResultPage` |
| `409` | 이메일 중복 | "이미 가입된 이메일입니다." | `RegisterPage` |
| `500` | 서버 오류 | "서버 오류. 잠시 후 다시 시도해 주세요" + 재시도 | `ErrorAlert` |
| 네트워크 오류 | 응답 없음 | "네트워크 연결을 확인해주세요." + 재시도 | `ErrorAlert` |

---

## 6. 컴포넌트 구현 골격

### 6.1 `SurveyPage` (SCR-03/04 공용)

```jsx
// src/pages/Survey/index.jsx

import { useState, useEffect, useCallback } from 'react';
import { useNavigate, Navigate } from 'react-router-dom';
import { toast } from 'sonner';
import { useSurveyStore } from '@/store/surveyStore';
import { useExamStore } from '@/store/examStore';
import { calcFirstScore, calcCScores } from '@/utils/kdsq';
import { KDSQ_P_SECOND_THRESHOLD, ANSWER_OPTIONS } from '@/constants/kdsq';
import LoadingSpinner from '@/components/common/LoadingSpinner';
import ErrorAlert from '@/components/common/ErrorAlert';
import SurveyHeader from './components/SurveyHeader';
import ProgressBar from './components/ProgressBar';
import QuestionCard from './components/QuestionCard';
import AnswerOptions from './components/AnswerOptions';
import SurveyNavigation from './components/SurveyNavigation';
import SecondExamDialog from './components/SecondExamDialog';

export default function SurveyPage({ type }) {

  // ── 1. 로컬 상태 ─────────────────────────────
  const [currentIndex, setCurrentIndex] = useState(0);
  const [answers, setAnswers] = useState([]);
  const [validationError, setValidationError] = useState('');
  const [isSecondDialogOpen, setIsSecondDialogOpen] = useState(false);
  const [pFirstScore, setPFirstScore] = useState(null);

  // ── 2. Zustand 전역 상태 ─────────────────────
  const { questions, loading, error, fetchQuestions } = useSurveyStore();
  const { submitting, submitP, submitC, pendingPExamId } = useExamStore();
  const navigate = useNavigate();

  // 2차 검사 진입 가드: 1차 4점 이상이 아니면 접근 불가
  const isBlocked = type === 'C' && !pendingPExamId;

  // ── 3. 사이드 이펙트 (데이터 로드) ───────────
  useEffect(() => {
    if (isBlocked) return;
    fetchQuestions(type);
  }, [type, isBlocked, fetchQuestions]);

  useEffect(() => {
    setAnswers(Array(questions.length).fill(null)); // 문항 로드 후 응답 배열 초기화
    setCurrentIndex(0);
  }, [questions]);

  // ── 4. 이벤트 핸들러 ─────────────────────────
  const handleSelect = useCallback((value) => {
    setAnswers((prev) => prev.map((a, i) => (i === currentIndex ? value : a)));
    setValidationError('');
  }, [currentIndex]);

  const isAnswered = () => {
    if (answers[currentIndex] === null) {
      setValidationError('답변을 선택해주세요.');
      return false;
    }
    return true;
  };

  const handlePrev = () => setCurrentIndex((i) => Math.max(0, i - 1));

  const handleNext = () => {
    if (!isAnswered()) return;
    setCurrentIndex((i) => i + 1);
  };

  const handleSubmit = async () => {
    if (!isAnswered()) return;
    try {
      if (type === 'P') {
        const exam = await submitP(calcFirstScore(answers));
        if (exam.firstScore >= KDSQ_P_SECOND_THRESHOLD) {
          setPFirstScore(exam.firstScore);
          setIsSecondDialogOpen(true);          // 4~10점 → 2차 안내
        } else {
          navigate(`/results/${exam.examId}`);  // 0~3점 → 정상 결과
        }
      } else {
        const exam = await submitC(calcCScores(answers));
        navigate(`/results/${exam.examId}`);
      }
    } catch {
      toast.error('제출에 실패했습니다. 잠시 후 다시 시도해 주세요.');
    }
  };

  // ── 5. 조건부 렌더링 (가드/로딩/에러/빈 데이터) ───
  if (isBlocked) {
    toast.info('1차 검사(KDSQ-P)를 먼저 진행해주세요.');
    return <Navigate to="/surveys" replace />;
  }
  if (loading) return <LoadingSpinner message="문항을 불러오는 중..." fullScreen />;
  if (error) return <ErrorAlert message={error} onRetry={() => fetchQuestions(type)} />;
  if (questions.length === 0) return null;

  const question = questions[currentIndex];

  // ── 6. 렌더링 ────────────────────────────────
  return (
    <div className="container mx-auto max-w-2xl py-6 space-y-6">
      <SurveyHeader type={type} />
      <ProgressBar current={currentIndex + 1} total={questions.length} />
      <QuestionCard question={question} number={currentIndex + 1} error={validationError}>
        <AnswerOptions
          name={`q-${question.id}`}
          options={ANSWER_OPTIONS}
          value={answers[currentIndex]}
          onChange={handleSelect}
        />
      </QuestionCard>
      <SurveyNavigation
        isFirst={currentIndex === 0}
        isLast={currentIndex === questions.length - 1}
        submitting={submitting}
        onPrev={handlePrev}
        onNext={handleNext}
        onSubmit={handleSubmit}
      />
      {type === 'P' && (
        <SecondExamDialog
          open={isSecondDialogOpen}
          firstScore={pFirstScore}
          onConfirm={() => navigate('/surveys/c')}
          onLater={() => navigate('/mypage')}
        />
      )}
    </div>
  );
}
```

### 6.2 `ResultPage` (SCR-05)

```jsx
// src/pages/Result/index.jsx

import { useEffect } from 'react';
import { useParams, useNavigate } from 'react-router-dom';
import { useExamStore } from '@/store/examStore';
import { getTotalScore } from '@/utils/kdsq';
import { MAX_SCORE } from '@/constants/kdsq';
import LoadingSpinner from '@/components/common/LoadingSpinner';
import ErrorAlert from '@/components/common/ErrorAlert';
import ResultHeader from './components/ResultHeader';
import ResultStatus from './components/ResultStatus';
import TotalScore from './components/TotalScore';
import ScoreSummary from './components/ScoreSummary';
import ExamDate from './components/ExamDate';
import Recommendation from './components/Recommendation';
import HistoryButton from './components/HistoryButton';

export default function ResultPage() {

  // ── 1. 로컬 상태 ─────────────────────────────
  const { examId } = useParams();

  // ── 2. Zustand 전역 상태 ─────────────────────
  const { currentExam, loading, error, fetchExam } = useExamStore();
  const navigate = useNavigate();

  // ── 3. 사이드 이펙트 ─────────────────────────
  useEffect(() => {
    // 제출 직후라면 store에 같은 결과가 있으므로 재조회 생략
    if (currentExam?.examId !== Number(examId)) fetchExam(examId);
  }, [examId]);

  // ── 5. 조건부 렌더링 ─────────────────────────
  if (loading || !currentExam) return <LoadingSpinner message="결과를 불러오는 중..." fullScreen />;
  if (error) return <ErrorAlert message={error} onRetry={() => fetchExam(examId)} />;

  const exam = currentExam;
  const isC = exam.examType === 'KDSQ_C';

  // ── 6. 렌더링 ────────────────────────────────
  return (
    <div className="container mx-auto max-w-2xl py-6 space-y-6">
      <ResultHeader />
      <ResultStatus exam={exam} />
      <TotalScore score={getTotalScore(exam)} maxScore={MAX_SCORE[exam.examType]} />
      {isC && (
        <ScoreSummary
          memoryScore={exam.memoryScore}
          otherScore={exam.otherScore}
          adlScore={exam.adlScore}
        />
      )}
      <ExamDate date={exam.createdAt} />
      {isC && <Recommendation riskLevel={exam.riskLevel} />}
      <HistoryButton onClick={() => navigate('/mypage')} />
    </div>
  );
}
```

### 6.3 `MyPage` (SCR-06)

```jsx
// src/pages/MyPage/index.jsx

import { useEffect, useMemo } from 'react';
import { useNavigate } from 'react-router-dom';
import { useMemberStore } from '@/store/memberStore';
import { useExamStore } from '@/store/examStore';
import LoadingSpinner from '@/components/common/LoadingSpinner';
import ErrorAlert from '@/components/common/ErrorAlert';
import ProfileCard from './components/ProfileCard';
import ExamHistoryTable from './components/ExamHistoryTable';
import ExamTrendChart from './components/ExamTrendChart';
import EmptyHistory from './components/EmptyHistory';

export default function MyPage() {

  // ── 2. Zustand 전역 상태 ─────────────────────
  const { me, loading: meLoading, error: meError, fetchMe } = useMemberStore();
  const { history, loading: historyLoading, error: historyError, fetchHistory } = useExamStore();
  const navigate = useNavigate();

  // ── 3. 사이드 이펙트 ─────────────────────────
  const loadAll = () => { fetchMe(); fetchHistory(); };
  useEffect(() => { loadAll(); }, []);

  // 테이블은 최신순, 차트는 오래된 순
  const sortedDesc = useMemo(
    () => [...history].sort((a, b) => b.createdAt.localeCompare(a.createdAt)),
    [history]
  );
  const sortedAsc = useMemo(() => [...sortedDesc].reverse(), [sortedDesc]);

  // ── 5. 조건부 렌더링 ─────────────────────────
  if (meLoading || historyLoading) return <LoadingSpinner fullScreen />;
  if (meError || historyError) {
    return <ErrorAlert message={meError || historyError} onRetry={loadAll} />;
  }

  // ── 6. 렌더링 ────────────────────────────────
  return (
    <div className="container mx-auto max-w-3xl py-6 space-y-6">
      <h1 className="text-2xl font-bold">마이페이지</h1>
      {me && <ProfileCard member={me} />}

      <section className="space-y-4">
        <div className="flex justify-between">
          <h2 className="text-xl font-semibold">검사 이력</h2>
          <span className="text-sm text-neutral-500">총 {history.length}건</span>
        </div>
        {history.length === 0 ? (
          <EmptyHistory onStart={() => navigate('/surveys')} />
        ) : (
          <>
            <ExamHistoryTable exams={sortedDesc} onDetail={(id) => navigate(`/results/${id}`)} />
            <h2 className="text-xl font-semibold">검사 결과 추이</h2>
            <ExamTrendChart exams={sortedAsc} />
          </>
        )}
      </section>
    </div>
  );
}
```

### 6.4 스토어 골격 — `examStore`

```javascript
// src/store/examStore.js
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';
import { examApi } from '@/api/examApi';
import { KDSQ_P_SECOND_THRESHOLD } from '@/constants/kdsq';

export const useExamStore = create(
  persist(
    (set, get) => ({
      currentExam: null,
      history: [],
      pendingPExamId: null,
      submitting: false,
      loading: false,
      error: null,

      submitP: async (firstScore) => {
        set({ submitting: true });
        try {
          const exam = await examApi.submitP({ firstScore });
          set({
            currentExam: exam,
            pendingPExamId: exam.firstScore >= KDSQ_P_SECOND_THRESHOLD ? exam.examId : null,
          });
          return exam;
        } finally {
          set({ submitting: false });
        }
      },

      submitC: async (scores) => {
        set({ submitting: true });
        try {
          const exam = await examApi.submitC({ pExamId: get().pendingPExamId, ...scores });
          set({ currentExam: exam, pendingPExamId: null });
          return exam;
        } finally {
          set({ submitting: false });
        }
      },

      fetchExam: async (examId) => {
        set({ loading: true, error: null });
        try {
          set({ currentExam: await examApi.getExam(examId) });
        } catch (e) {
          set({ error: e.message ?? '결과를 불러오지 못했습니다.' });
        } finally {
          set({ loading: false });
        }
      },

      fetchHistory: async () => {
        set({ loading: true, error: null });
        try {
          set({ history: await examApi.getHistory() });
        } catch (e) {
          set({ error: e.message ?? '검사 이력을 불러오지 못했습니다.' });
        } finally {
          set({ loading: false });
        }
      },

      reset: () => set({ currentExam: null, history: [], pendingPExamId: null, error: null }),
    }),
    {
      name: 'kdsq-exam',
      storage: createJSONStorage(() => sessionStorage),
      partialize: (state) => ({ pendingPExamId: state.pendingPExamId }), // 2차 진입 자격만 유지
    }
  )
);
```

### 6.5 API 클라이언트 — `api/client.js`

```javascript
import axios from 'axios';
import { toast } from 'sonner';
import { useAuthStore } from '@/store/authStore';

export const client = axios.create({ baseURL: import.meta.env.VITE_API_BASE_URL });

// 요청: 토큰 자동 첨부
client.interceptors.request.use((config) => {
  const { token } = useAuthStore.getState();
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

// 응답: data 언래핑 + 401 공통 처리 (로그인 API 제외)
client.interceptors.response.use(
  (res) => res.data.data,
  (err) => {
    const status = err.response?.status;
    const isLoginApi = err.config?.url?.includes('/api/auth/login');
    if (status === 401 && !isLoginApi) {
      useAuthStore.getState().logout();
      toast.error('다시 로그인해주세요.');
      window.location.href = '/login';
    }
    return Promise.reject({
      status,
      message: err.response?.data?.message ?? '서버 오류. 잠시 후 다시 시도해 주세요',
    });
  }
);
```

### 6.6 설계 확인 필요 사항 (백엔드·팀 협의)

| # | 항목 | 본 설계의 가정 | 확인 대상 |
|---|---|---|---|
| 1 | 점수 계산 위치 | 프론트에서 `firstScore`, 영역별 점수를 계산해 전송 (문항별 answers 미전송) | 백엔드 API 명세 |
| 2 | P–C 결과 연결 | `POST /api/exams/c`에 `pExamId` 전달 | 백엔드 API 명세 / ERD |
| 3 | 1차 4점 이상 후 2차 미진행 | `[나중에 하기]` 시 `/mypage` 이동, sessionStorage 유지 동안 재진입 가능. 이력에는 "2차 대상"으로 표시 | 기획 |
| 4 | 문항 선택지 | 0/1/2점 3지선다를 프론트 상수로 관리 | 문항 API 응답 형태 |
| 5 | 토큰 저장 | localStorage (persist). 보안 강화 시 httpOnly 쿠키 방식 검토 | 백엔드 인증 방식 |

---

## 7. 성능 최적화

| 최적화 기법 | 적용 대상 | 적용 이유 |
|---|---|---|
| `React.memo` | `QuestionCard`, `AnswerOptions`, `ProgressBar`, `SurveyHeader` | 응답 선택 시 `SurveyPage` 전체가 리렌더링되지만 헤더·진행률 Props는 그대로 |
| `React.memo` | `ExamTrendChart` | 차트 렌더링 비용이 커서 `exams` 변경 시에만 다시 그림 |
| `useCallback` | `SurveyPage.handleSelect` | `React.memo`된 `AnswerOptions`에 함수 Props 전달 |
| `useMemo` | `MyPage` 이력 정렬(`sortedDesc`, `sortedAsc`) | 매 렌더링마다 배열 복사·정렬 방지 |
| `useMemo` | `ExamTrendChart` 차트 데이터 변환 | P/C 라인 분리 데이터 가공 |
| 재조회 생략 | `ResultPage` | 제출 직후 `currentExam` 재사용으로 불필요한 API 호출 방지 |

- [x] `React.memo` 적용
- [x] `useCallback` 적용
- [x] `useMemo` 적용

---

## 8. 작성 체크리스트

- [x] STEP 1: 와이어프레임에서 컴포넌트 경계를 사각형으로 그렸는가? (SCR-01 ~ 06)
- [x] STEP 2: 컴포넌트 트리(부모-자식 계층)가 작성되었는가?
- [x] STEP 3: 각 상태가 왜 로컬/전역인지 근거가 있는가?
- [x] 모든 Props의 타입, 필수여부, 기본값이 작성되었는가?
- [x] 사용하는 API와 호출 시점이 명확한가?
- [x] 로딩 / 에러 / 빈 데이터 3가지 상태가 모두 고려되었는가?
- [x] UI 설계서 판정 기준(P 0~3/4~10, C 0~5/6~30) 및 `totalScore` 프론트 계산 정책을 반영했는가?
- [ ] 6.6 확인 필요 사항을 백엔드 API 명세와 대조했는가?